name: Temporary Douyin Real Page Probe

on:
  pull_request:
    branches:
      - main
    paths:
      - "tmp/douyin-background-export/**"
      - ".github/workflows/tmp-douyin-background-export.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  douyin-page-probe:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout temporary branch
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install real browser
        run: |
          python -m pip install --upgrade pip
          python -m pip install playwright
          python -m playwright install --with-deps chromium

      - name: Open Douyin work and intercept full media data
        run: python tmp/douyin-background-export/douyin_page_probe.py

      - name: Show captured files
        if: always()
        run: find tmp/douyin-background-export/douyin_page_probe_output -maxdepth 3 -type f -printf "%p %s bytes\n" | sort || true

      - name: Upload captured Douyin data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: douyin-real-page-data
          path: tmp/douyin-background-export/douyin_page_probe_output/**
          if-no-files-found: warn
          compression-level: 0
          retention-days: 1
